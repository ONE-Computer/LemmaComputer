# Hosted deployment marker.
#
# Deployment policy is selected only by ONECOMPUTER_INSTALLATION_KIND in the
# canonical environment contract. Before using this file, run:
#   npm run env:check -- --profile=hosted
#
# The base Compose file consumes generated per-service environment projections;
# this overlay deliberately has no environment values to override.
services: {}
